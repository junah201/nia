import './App.css';
import Provider from '@/shared/Providers';
import Router from '@/shared/Router';

function App() {
  return (
    <Provider>
      <Router />
    </Provider>
  );
}

export default App;
